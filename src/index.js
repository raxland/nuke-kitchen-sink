import { createElement, render } from 'rax';

render(
  <div>
    <h1>Nuke Kichen Sink app</h1>
    <p>There is no real app here. This whole repo is collection of stories to demo Nuke UI framework</p>
  </div>
);
